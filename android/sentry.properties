defaults.url=https://sentry.io/
defaults.org=krontiva-africa
defaults.project=scarnergy-app
# auth.token deliberately not set here — comes from the SENTRY_AUTH_TOKEN
# EAS secret (project scope, "production" environment) at build time, so it
# never lands in the repo in plaintext.
